<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lgyun.system.user.mapper.RunCompanyMapper">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.lgyun.system.user.entity.RunCompanyEntity" id="runCompanyMap">
        <result property="id" column="id"/>
        <result property="makerId" column="maker_id"/>
        <result property="companyName" column="company_name"/>
        <result property="employeeName" column="employee_name"/>
        <result property="taxNo" column="tax_no"/>
        <result property="bankName" column="bank_name"/>
        <result property="bankAccount" column="bank_account"/>
        <result property="contacterName" column="contacter_name"/>
        <result property="phoneNo" column="phone_no"/>
        <result property="emailAddress" column="email_address"/>
        <result property="setDate" column="set_date"/>
        <result property="memoInfo" column="memo_info"/>
        <result property="createUser" column="create_user"/>
        <result property="createTime" column="create_time"/>
        <result property="updateUser" column="update_user"/>
        <result property="updateTime" column="update_time"/>
        <result property="status" column="status"/>
        <result property="isDeleted" column="is_deleted"/>
    </resultMap>

    <select id="findCompanyName" resultMap="runCompanyMap">
        select *from diyi_run_company where is_deleted = 0 and maker_id =#{param1}
    </select>

    <select id="repeatCompanyName" resultMap="runCompanyMap">
        select *from diyi_run_company where is_deleted = 0 and company_name=#{param1} and maker_id =#{param2}
    </select>

    <select id="repeatTaxNo" resultMap="runCompanyMap">
        select *from diyi_run_company where is_deleted = 0 and tax_no=#{param1} and maker_id =#{param2}
    </select>
    <select id="repeatBankAccount" resultMap="runCompanyMap">
        select *from diyi_run_company where is_deleted = 0 and bank_account=#{param1} and maker_id =#{param2}
    </select>
</mapper>
