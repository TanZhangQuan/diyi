#服务器端口
server:
  port: 8102

spring:
  profiles:
    active: prod
  application:
    name: diyi-user

logging:
  level:
    com.alibaba.nacos: error
  file:
    name: diyi-user.log

feign:
  client:
    config:
      # feign全局日志级别
      default:
        loggerLevel: full


