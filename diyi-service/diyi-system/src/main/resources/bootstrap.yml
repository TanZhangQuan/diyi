#服务器端口
server:
  port: 8106

spring:
  profiles:
    active: prod
  application:
    name: diyi-system

logging:
  level:
    com.alibaba.nacos: error
    com.lgyun.system.user.feign: debug
  file:
    name: diyi-system.log

feign:
  client:
    config:
      # feign全局日志级别
      default:
        loggerLevel: full
